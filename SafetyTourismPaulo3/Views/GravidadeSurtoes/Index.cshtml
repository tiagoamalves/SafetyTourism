@model IEnumerable<SafetyTourism.Models.GravidadeSurto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataRegisto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NivelGravidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataRegisto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NivelGravidade)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GravidadeSurtoID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GravidadeSurtoID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GravidadeSurtoID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
